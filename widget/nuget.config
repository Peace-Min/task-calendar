<?xml version="1.0" encoding="utf-8"?>
<!-- 폐쇄망 오프라인 빌드: 원격 소스(nuget.org)만 비워 타임아웃 방지.
     SDK 팩 경로(fallbackPackageFolders)와 전역 캐시(~/.nuget/packages)는 그대로 사용. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
