# 로드맵 · 지금 어디에 있고 다음이 무엇인가 (ROADMAP)

> **기준일 2026-09-02.** 2026-07-08판을 폐기하고 새로 썼다 — 옛 판은 "API 서버 → 위젯 DB 전환 + 오프라인 캐시" 순서를 전제했는데, 실제는 **위젯이 MySQL 직결로 먼저 전환했고 캐시는 두지 않았다**(§1). 낡은 순서를 근거로 작업을 시작하는 일을 막기 위해 통째로 교체한다.
>
> 상세 백로그는 여기 두지 않는다 — DB 작업은 [db/ROADMAP.md](../db/ROADMAP.md), 테스트는 [tests/README.md](../tests/README.md) 백로그 절, UI는 [UIUX-ROADMAP.md](UIUX-ROADMAP.md). 이 문서는 **"지금 어디인가 · 왜 계획이 바뀌었나 · 다음 릴리스에 무엇을 해야 하나"** 세 가지만 답한다.

---

## 0. 지금 어디에 있나

### 릴리스 라인
| 브랜치 | 버전 | 상태 |
|---|---|---|
| `feat/db-app` | **0.18.0** (2026-09-02 승격 `d5bb5c0` · 인스톨러 0.15~0.18.0 빌드됨) | **실제 릴리스 라인.** main을 전부 포함 |
| `main` | 0.12.0 (2026-07-21) | **동결.** 그 뒤 docs 커밋 2건(08-31)만 있고 db에 미반영 |

> **구조적 문제**: 문서·CI·패치노트 체계가 `main`을 트렁크로 전제하는데 실제 트렁크는 `feat/db-app`이다. §5의 첫 번째 결정.

### 완료된 것
- **데스크톱 위젯** — WPF .NET9 + WebView2, 단일 HTML 임베드. 바탕화면 상주·트레이·미리알림·Git/SVN 커밋 수집·회사 보고(netcus) 자동 전송·자동 업데이트. 0.12.0에서 UI/UX 6관점 정합 완료.
- **과제 카탈로그 DB** (축 A P0~P4) — `project`·`customer`·코드테이블, 앱에서 CRUD, Excel 추출. 로컬 MySQL에서 검증 완료.
- **캘린더 데이터 DB 전환** (축 B C1~C3.6) — `cal_*` 16표(schema_version 8), 읽기(`CalendarDb`)·쓰기(`CalendarWriteDb`) 배선, **XML 은퇴**(`DeployConfig.XmlRetired = true`, 2026-09-01), 이관 도구(`db/deploy/xml-to-db`), 명시적 가져오기.
- **보고 기록 DB** — `cal_report_daily/hours/weekly`(2026-08-31): 캘린더가 보낸 것을 DB에 남긴다.
- **타인 일정 열람** (C4, 2026-09-03) — 「구성원」 행 클릭 = **읽기 전용** 열람. 권한은 호스트가 다시 판정하고
  (`ProjectDb.CanViewScheduleAsync` 한 벌), 일정만 보낸다(메모·커밋·할 일·공수·근태 제외).

### 미완
- API 서버 (없음 — 각 PC 위젯이 MySQL 직결)
- same-origin 정적 배포 (브라우저 버전)
- ~~타인 일정 열람 (C4)~~ → **2026-09-03 완료.** 「구성원」 행 클릭 = 읽기 전용 열람.
  권한은 호스트가 다시 판정하고(`ProjectDb.CanViewScheduleAsync` 한 벌), 일정만 보낸다(메모·커밋·할 일 제외).
  게이트 `tests/loop-peer-view.mjs` 19/19 — 거부 5갈래를 따로 세운다

### 품질 수준 — 2026-09-02 6관점 감사
| 관점 | 점수 | 한 줄 |
|---|---|---|
| 아키텍처 | 6 | 배치 규칙(도메인=JS/능력=C#)은 지켜짐. `if(HOST)` 47곳·`bind()` 985줄·인계 문서가 XML 시대 |
| 문서 | 5 | 최신 결정(db/ROADMAP·설계 §11)은 정확. README/USAGE/CHANGELOG/DEPLOY/ARCHITECTURE는 7월에 멈춤 |
| DB 스키마 | **7** | 상용에 가까움. 재구축본=누적본 0건 차이 실측 |
| **배포 적합성** | **5 — not_ready** | 감사 시점 P0 3건 → **09-02 전부 해소**(§2-1). §2-2 P1 8건 중 7건 완료(+ 업데이트 채널 ACL), **1건 남음**(자격증명 — 배포 PC에서 1회 교체). P1 회귀는 loop-schema-gate.mjs로 상시 재증명 |
| 테스트 | 6 | JS 계약은 잘 잠금. C# 9,423줄 실행 테스트 0건 |
| 보안 | 6 | SQL 바인딩·XSS·DPAPI 확인. 업데이트 체인 무인증 |

기준: 상용 8+ · 사내 배포 가능 6+ · 위험 4↓. 58건 중 22건이 적대검증(3명 반박) 생존, 36건은 미검증(file:line 근거는 있음).

---

## 1. 왜 원래 계획과 달라졌나 — 정직한 기록

**원래(2026-07-08)**: T1(netcus 서버에 정적 배포 가능?) → T2(조직이 채택·서버 승인?) → **API 서버 + DB + 로그인** → 그 뒤에 위젯을 DB 클라이언트로 전환하되 **로컬 캐시 + 동기화(offline-first)** 로.

**실제(2026-08~09)**: T2를 기다리지 않고 **위젯이 MySQL에 직결**로 먼저 전환. API 서버 없음. **온라인 전용, 로컬 캐시 없음**(ADR-18, [CALENDAR-TABLE-DESIGN §2](../db/CALENDAR-TABLE-DESIGN.md)).

**왜**: 조직 채택은 개인이 못 만든다. 그 전에 "다른 자리 PC에서 내 일정 보기"·"위젯↔브라우저 따로 노는 문제"·"남의 일정 열람"을 풀려면 DB가 먼저 있어야 했고, API 없이도 폐쇄망 MySQL 하나면 된다. [DECISIONS 결정4 갱신](DECISIONS.md)이 이 경로를 **"클라이언트가 DB에 직접 붙는 중간 단계"** 로 이미 인정하고 있었다.

**그 대가 — 지금 안고 있는 것**:
| 대가 | 어디에 기록 | 상태 |
|---|---|---|
| 앱 계정 비번이 exe에 상수로 내장, 80대에 배포 | USER-LOGIN §3.4 | 문서화된 한계. 완화책 3중 권한 최소화만 됨 |
| 신뢰 경계가 API가 아니라 DB GRANT | 결정4 갱신 | 중간 단계 한정으로 수용 |
| 서버 못 붙는 날 **미리알림 0건** (예전엔 XML이라 항상 울림) | 설계 §4, db/ROADMAP C5 | **결정(09-02): 수용.** 로컬 알림표는 두 번째 진실이 되므로 두지 않는다(§5-2). 릴리스 노트에 명시 |
| 부팅 실패 시 [다시 시도] 없음 | 설계 §2가 확정한 것과 다름 | 코드가 결정을 어김 |

> 옛 로드맵의 "오프라인 캐시" 서술은 **API 서버 시대(§4)에 다시 논의할 것**이지, 지금 캐시를 만들 근거가 아니다.

---

## 2. 다음 — **0.18.0 = DB 전환 완료 버전. 배포는 검증 뒤(결정 5)**

**배포를 서두르지 않는다.** 순서는 **캘린더 데이터 이관 → 충분한 테스트 → 환경 구축 → 배포**이고, 각 단계가 끝나야 다음으로 간다(결정 5, 2026-09-02). 배포 시점이 오면 폐쇄망 서버 MySQL에 스키마를 올리고 위젯을 그 서버로 향하게 빌드하되 **한 번에 80명이 아니라 파일럿 3~5명 → 확대.** `XmlRetired=true`라 되돌리기가 원본 `data.xml`(가져오기 후에도 지우지 않음)로 되돌리는 것뿐이므로 그 절차도 파일럿에서 리허설한다.

### 2-0. 트렁크 — 결정 1(§5)에 따라 **승격 없음**
- `feat/db-app`이 릴리스 라인으로 계속 간다. main의 docs 커밋 2건(08-31): `5d371c6`(테스트 백로그)은 cherry-pick 완료(`4953c30`), `a90e56c`(옛 로드맵 주석)는 로드맵 재작성으로 불필요 — **main 잔여 0건.**
- [ ] (선택) CI — `ci.yml` push 브랜치에 `feat/**` 한 줄 추가하면 승격 없이도 돈다. 지금은 97커밋이 CI 미경유.

### 2-1. 배포 차단 해소 — P0 3건 *(각 S, 합쳐 하루)*

> ✅ **2026-09-02 완료** — P0-3·P0-2(로컬 리허설) `a066d7b` · P0-1 `d5bb5c0`. P0-3는 명부만이 아니라 가려져 있던 게이트 결함 2건(FK 파서 순서·CHECK probe)까지 나와 함께 고침. **P0-2의 서버 설치(`-Install`)는 배포 PC에서** — 절차는 `db/deploy/README` 백업 절. 0.18.0 로컬 빌드는 게이트용(DbHost=localhost), 실배포 빌드는 배포 PC에서.

| # | 무엇 | 왜 차단인가 | 닫는 조건 |
|---|---|---|---|
| **P0-1** | **0.18.0 버전 승격** + 패치노트("XML→DB 전환") + 인스톨러 + sha256 게이트 | DB 전환(09-01)까지 반영됐는데 버전이 08-04의 0.17.1 그대로. 이 번호로 배포하면 `Update.cs`의 `mver > own`이 거짓이라 **이미 0.17.1이 깔린 PC는 영영 갱신 안 됨** | CLAUDE.md 릴리스 체크리스트 전항 통과 |
| **P0-2** | **백업 설치 + 복구 리허설** | 설계 §11이 "이관·앱 전환보다 **먼저**"라 했는데 전환이 먼저 나감. `taskmgr_backup` 계정·`.cnf`·스케줄·첫 덤프 전부 없음. `taskmgr_app`이 전 사용자 행 DELETE 권한을 갖고 80대에 배포되는데 되돌릴 스냅샷이 0회 | `create-backup-user.sql` → `.cnf` → 수동 1회 → **임시 DB에 복원해 16표·행수 대조** → `-Install` → 일자·결과 기록 |
| **P0-3** | **`init-calendar.ps1` 명부 13 → 16** | 원큐 구축 진입점이 현재 스키마에서 `Die`(exit 1). 문서는 "게이트 0 통과"로 기록. 우회해 SQL 직접 돌리면 게이트가 통째로 빠짐 | 격리 DB에서 `init-calendar.cmd` exit 0 실측 |

### 2-2. 배포 전 조건 — P1 *(S 위주, 2~3일)*
- [x] **낡은 클라이언트 쓰기 차단** — `CalendarDb.ExpectedSchemaVersion` 상수 + `SaveAsync`가 전량 교체(가져오기·초기화)만 거부(통상 저장은 §5.5대로 허용). 웹 배지 경고·사전 차단. 서버 v9 올려 실위젯 11/11 확인. `f6d6c2d`
- [x] **부팅 실패 [다시 시도]** + 자동 백오프(15·30·60s) — 주입형 `createBootRetry`, retryable=false(로그인 없음·미등록)면 무재시도, 성공 시 해제. 실위젯 카운트다운·수동/자동 확인. `f6d6c2d`
- [x] **미리알림 릴리스 노트** — 결정(§5-2)대로 구현 없음. 0.18.0 패치노트에 "그날 미리알림도 울리지 않습니다" 반영(`d5bb5c0`)
- [ ] **자격증명** — 배포 직전 `DeployConfig.cs`·`create-app-user.sql` 둘 다 **같은 강한 값**으로 교체(영구값이라 한 번만). 주입 기계는 안 만듦(§5-3). 호스트 `%` → 사내 대역은 선택
- [x] **테스트 게이트 정직화** — jsdom 없으면 Layer 2 4파일(222건)이 조용히 사라진 채 exit 0이던 것 → skip 사유별 집계 + exit 2, `TC_TEST_STRICT=1`이면 fail(exit 1). 릴리스 게이트(CLAUDE.md 5번)를 strict로 변경. `da1b7fd`
- [x] **마이그레이션 정합 가드** — `tests/schema-guards.test.mjs` 9종(DROP⊇CREATE·순서 양방향·migrate 체인 연속·정본 버전 일치) + 변이 9종. 정본에서 DROP 한 줄 빼면 실제로 빨간불 확인. `da1b7fd`
- [x] **업데이트 채널** — 공유 위치 구성 절(update/README §공유 위치 구성)에 ACL "모두 읽기 / 배포자만 쓰기"를 필수 항목으로 명시(P1-7). 해시는 무결성이지 출처가 아니라는 근거 포함
- [x] **가져오기 후 원본 개명** — `RenameImportedSource()`: 이관 성공 뒤 `data.xml.migrated-<yyyyMMdd>`(데이터 폴더 안만·덮어쓰기 금지·개명 실패는 가져오기 실패 아님). 실 파일대화상자 9/9 확인. 설계 §8도 구현으로 정정. `f6d6c2d`

### 2-3. 파일럿 → 확대
- [ ] 3~5명: 서버 DB + 0.18.0. 확인할 것 — 백업 주기 실동작 · 아침 부팅 순서 · 미리알림 · 원본 `data.xml`로 되돌리기 리허설
- [ ] 80명 확대. 업데이트 소스를 개인 PC FTP에서 서버로 옮기는 **주소 이관 순서**(새 위치 게시 → 옛 위치에 마지막 버전 → 전원 전환 확인 → 옛 위치 내림)

---

## 3. 배포 후 — 유지보수 부채 *(M~L)*

감사에서 "사내 배포 가능(6)이지 상용(8+) 아님"의 원인. 배포를 막지는 않지만 두 번째 개발자 합류를 막는다.

| 부채 | 크기 | 왜 |
|---|---|---|
| ~~**인계 문서 일괄 갱신**~~ ✅ **2026-09-02 완료** — 13파일(`a22928a`) + 스키마 정본 모순 4곳·수기 HTML 정리(`f6fae7b`) + widget/update README(`8b23d2e`). 전수 인벤토리 42개 대조. `db/SETUP.md`·`schema-overview.html` 폐기 | M | `DEPLOY.md`는 의도적 공백(§5-4)이라 제외. 수기 HTML 3개는 생성기가 없어 배너로만 — 스키마 변경 시 또 낡음 |
| `if(HOST)` 산탄 흡수 | M | 문서가 "걷어내겠다"고 한 뒤 15 → 47곳으로 증가. DB 카탈로그 코드에 '위젯 전용' 토스트 ~20회 복붙 |
| `bind()` 985줄 분할 | M | 전체 JS의 12%가 한 함수. 무관한 핸들러 201건 공존 |
| **C# 실행 테스트 도입** | M | 9,423줄에 0건(전부 regex 문자열 검사). Reminders·Update·RepoPaths·XlsxWriter·NetcusText부터 |
| 쓰기 모델 차분화 | M | 편집마다 상태 전체 재기록. 5년치면 편집 1회 = 수천 문장, 20s 타임아웃. ★미실측(지금 23건) |
| `cal_report_hours.cat_no` | S | FK 없이 재사용 번호 저장 → 과제 삭제 후 신규가 같은 번호 받으면 과거 보고 시간이 새 과제로 귀속 |
| 접속문자열 빌더 4벌·발주처↔코드값 블록 복제·고아 자격증명 코드 | S | 한쪽만 고쳐진 드리프트 이미 관찰됨 |
| 테스트가 함수 이름을 텍스트로 잠금(`extractFunction` 17파일) | M | 위 리팩토링을 하면 동작 불변이어도 깨짐 — **부채 상환 전에 이것부터** |

---

## 4. 그 다음 — 원래 계획의 나머지 (게이트는 유지)

옛 로드맵의 1·2·4단계는 폐기가 아니라 **뒤로 밀린 것**이다. 착수 스위치는 그대로:

| 게이트 | 질문 | 열리면 |
|---|---|---|
| **T1** | netcus.com 서버에 우리 정적 앱을 배포할 수 있는가? (리눅스+JSP라 자체 Tomcat일 공산) | **브라우저 버전** — `www.netcus.com/calendar/`에 정적 배포. same-origin이라 SOP 소멸, 사용자 netcus 세션 재사용, 무설치 |
| **T2** | 조직이 공식 보고 도구로 채택하고 서버·DB·인증을 승인하는가? | **API 서버** — 클라이언트 직결을 API 경유로. 신뢰 경계가 GRANT → API로 이동. 브라우저도 같은 API. 이때 오프라인 캐시 재논의 |
| — | 위 둘과 무관 | ~~**타인 일정 열람**(C4)~~ — **2026-09-03 완료**(읽기 전용 · 권한 호스트 재판정) |

> T2 전에 서버 API를 세우지 않는다 — 운영·SoR 책임은 개인이 질 수 없다. 로컬 MySQL 검증기는 T2를 얻기 위한 설득 수단이므로 허용(결정4).

---

## 5. 결정 — 2026-09-02 확정

| # | 결정 | 결론 | 근거 |
|---|---|---|---|
| 1 | **트렁크** | **유지.** `feat/db-app`을 main으로 승격하지 않는다 | 승격 자체에 의미가 없다. 단 CI가 이 브랜치에서 안 도는 것은 사실 — 필요해지면 `ci.yml`에 `feat/**` 한 줄로 해결(승격과 무관) |
| 2 | **미리알림 오프라인** (C5) | **수용.** 서버에서 읽은 데이터가 있을 때만 울린다. 로컬 알림표 없음 | 전 인원이 서버 DB 하나를 보는 구조에서 로컬 알림표는 **두 번째 진실**이 된다 — 다른 PC에서 지운 일정이 이 PC에서 울린다. 캐싱 자체가 유지보수 포인트. 릴리스 노트에 "서버 연결 실패 시 그날 알림 없음" 명시. 부팅 재시도([다시 시도]·백오프)는 데이터를 *받아오는* 수단이라 별개로 구현(2-2) |
| 3 | **앱 계정 비번** | **주입 기계 안 만듦.** 배포 직전 강한 값으로 한 번 교체 + `create-app-user.sql`↔`DeployConfig.cs` 일치를 배포 체크리스트에 | 폐쇄망이라 git 노출의 외부 위협은 낮다. 남는 실무 포인트 둘 — (a) 영구값(로테이션 = 재빌드 + 80대)이니 기본값 `taskmgr1234`는 안 됨 (b) 지금 SQL은 `CHANGE_ME_ON_DEPLOY`, config는 `taskmgr1234`라 어긋남 |
| 5 | **배포 시점** | **서두르지 않는다.** 캘린더 데이터 이관 → 충분한 테스트 → 환경 구축을 마친 뒤 배포 | 지금 급하게 배포할 이유가 없다. 2-2(P1)·2-3(파일럿)은 이 순서 안에서 진행. P1 코드 5건(낡은 클라이언트 차단·부팅 재시도·테스트 정직화·정합 가드·원본 개명)은 배포 속도가 아니라 **정확성·테스트 정직성**이 목적이라 그대로 유효 |
| 4 | **`DEPLOY.md` 공백** | **의도적.** 실배포 값(서버 IP·계정·경로·DB 단계)은 배포 PC에서 직접 채운다. 저장소에는 뼈대만 | 실배포 값을 git에 두지 않는다. 2026-09-02 감사가 "DB 언급 0건"으로 P1 지적했으나 **결함 아님** — 다음 감사·에이전트는 재지적하지 말 것 |

---

## 이 문서가 전제하는 결정
[DECISIONS.md](DECISIONS.md) 전체 + 그 뒤 추가된 것: **ADR-18** 온라인 전용(과제·캘린더 공통) · **결정4 갱신** 클라이언트 직결은 중간 단계 · **결정5** 보고는 서버 릴레이 안 함 · [CALENDAR-TABLE-DESIGN](../db/CALENDAR-TABLE-DESIGN.md) §2·§3·§8·§11.

## 관련 문서
- [ARCHITECTURE.md](ARCHITECTURE.md) — 계층 구조 (★ XML 시대 서술 잔존, §3 부채)
- [db/ROADMAP.md](../db/ROADMAP.md) — DB 작업 상세 백로그(축A P0~P8 · 축B C1~C5)
- [db/CALENDAR-TABLE-DESIGN.md](../db/CALENDAR-TABLE-DESIGN.md) — 캘린더 DB 설계 확정본
- [tests/README.md](../tests/README.md) — 테스트 하네스 + 백로그
- [UIUX-ROADMAP.md](UIUX-ROADMAP.md) — UI 전용
