# 이슈 로그 — 수행과제 캘린더

> 운영/버그 추적용. 가장 최신 이슈를 위에 추가한다. (기능 변경 이력은 [CHANGELOG.md](CHANGELOG.md), 창 모드 의사결정은 [창모드-조사-보고서.md](창모드-조사-보고서.md))

---

## #1 위젯이 "계속 잠김" — 변경이 반영되지 않음 / 먹통 (2026-06-15)

- **상태**: 🔎 **원인 규명 완료 · 사용자 수정 보류** (조치 미적용, 2026-06-16 기준)
- **우선순위**: 높음
- **유형**: 버그 — 배포/실행(자동시작)

### 증상 (사용자 보고)
- "여전히 계속 잠김." 위젯이 계속 잠긴/먹통 상태이며, 재빌드·재배포 후에도 동일하게 느껴짐.
- 정확한 증상 분류가 아직 미확정 — 아래 *확인 필요* 참고.

### 환경
- Windows 11 Pro (26200), .NET 9 Desktop 런타임, WebView2
- 형태: WPF + WebView2 단일 임베드 HTML, 바탕화면 최하위(bottom-most) 도구창 위젯
- 배포본(새 빌드): `C:\Users\CEO\Desktop\수행과제캘린더\수행과제캘린더.exe` — 자체포함 단일파일, 163MB, 2026-06-15 빌드

### 핵심 발견 — 자동시작 경로 불일치 + 단일 인스턴스 뮤텍스 충돌 (유력 원인)
- `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 자동시작 등록값:
  ```
  TaskCalendarWidget = "C:\Users\CEO\Desktop\console\task-calendar\widget\bin\Release\net9.0-windows\TaskCalendarWidget.exe"
  ```
  → **옛 빌드(프레임워크 종속, 구버전 bin\Release 출력)** 를 가리킴.
- 실제 사용/배포하는 exe는 `...\Desktop\수행과제캘린더\수행과제캘린더.exe` (**새 빌드**) 로 경로가 **다름**.
- 앱은 단일 인스턴스 뮤텍스 `TaskCalendarWidget_SingleInstance_2C8F` 를 사용(App.xaml.cs `OnStartup` — 이미 실행 중이면 `Shutdown()`).
- **가설**: 로그인 시 Windows 자동시작이 *옛 빌드*를 먼저 실행해 뮤텍스를 선점 → 사용자가 *새 exe*를 더블클릭해도 두 번째 인스턴스가 조용히 종료됨. 결과적으로 화면에는 항상 **옛 빌드만** 떠 있어 새 기능/수정이 전혀 반영되지 않음 → "계속 잠김(안 바뀜·먹통)"으로 체감.
- 현재 스냅샷: 실행 프로세스 = `수행과제캘린더`(PID 7240, Responding=True, 115MB). 옛 `TaskCalendarWidget`는 이번엔 미실행이나, **다음 로그인 때 자동시작이 옛 경로를 다시 실행**하게 되어 있음.

### 그 외 의심 원인 (로그/증상 확인 후 압축)
1. **자체포함 단일파일(163MB) 시작 지연** — 부팅마다 `%TEMP%\.net` 으로 네이티브 추출 → 첫 렌더까지 멈춘 듯 보임. (이 PC엔 .NET 9 런타임이 있으므로 *프레임워크 종속 다중파일* 빌드가 더 빠름)
2. **최하위 위젯 렌더 throttle** — 다른 창에 가려지면 WebView2/Chromium이 렌더를 줄여, Win+D로 드러낼 때 잠깐 멈춘 듯 보일 수 있음.
3. **WebView2 UserDataFolder 락** — 이전 인스턴스 잔존 시 `CoreWebView2` 초기화 지연.
4. **📌 부착(이동 잠금)** 상태를 "잠김"으로 오인했을 가능성.
5. **노트 드로어/스크림(신규, 2026-06-15)** 이 호스트바를 덮은 채 안 닫히는지 — Esc/바깥클릭 닫힘은 프리뷰에서 정상 확인됨.

### 첨부 로그
- ⚠ **원문 미확보**: 트랜스크립트 전체를 검색했으나 잠김 관련 로그/스크린샷이 이 세션에 캡처돼 있지 않음(사용자 기억 착오로 추정). **다만 원인은 레지스트리(HKCU\Run 값) + 코드(단일인스턴스 뮤텍스) 점검으로 독립 규명됨** → 로그 없이도 위 [핵심 발견]의 가설이 성립. 로그는 증상 재현 분류용으로만 추가 확보하면 됨(필수 아님).

### 확인 필요 (재현 정보)
- 잠김 시점: ① 시작 직후인지 ② 한참 사용 중인지 ③ Win+D로 바탕화면 볼 때인지?
- 작업관리자에서 해당 프로세스가 "응답 없음"으로 표시되는지? 프로세스명이 `TaskCalendarWidget`인지 `수행과제캘린더`인지?
- 마우스 클릭/이동 자체가 안 되는지, 아니면 화면(버전)이 안 바뀌는지?

### 제안 조치
- [ ] 자동시작 등록값을 새 배포 exe 경로로 교체(또는 자동시작 재설정), 옛 `bin\Release\...\TaskCalendarWidget.exe` 정리
- [ ] 부팅마다 옛 빌드가 뜨지 않는지 재현 확인
- [ ] (선택) 배포를 프레임워크 종속 빌드로 전환해 시작 지연 제거
- [ ] 로그/증상 확인 후 원인 확정
