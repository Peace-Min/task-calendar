# 창 노출 방식 조사 보고서 — 트레이 / 작업표시줄 / Alt+Tab / 항상위·아래 / 바탕화면 상주

> 작성 2026-06-16 · 방법: 다중 에이전트 워크플로(wjv9arrep, 4축 병렬 조사 → 종합 → 적대적 검증), 각 앱 WebSearch 확인 + MS 공식 문서 + 실제 코드(MainWindow.xaml.cs) 대조. 목적 = 또 다른 임의 결정이 아니라 **사용자가 직접 판단할 선택지·트레이드오프**.

---

## 0. 한 문장 결론 (가장 정직한 단서)

**Windows에는 "바탕화면에 항상 깔린 채로(항상 아래) 동시에 Alt+Tab으로 불러오는" 창 모드가 존재하지 않는다.**
OS 규칙상 Alt+Tab은 대상 창을 *활성화*하는 동작이고, 활성화는 반드시 그 창을 *맨 앞*으로 올린다 — 즉 "항상 아래"와 "Alt+Tab 호출"은 **상호 배타**다(Microsoft 공식 문서로 확인). 따라서 둘 중 하나를 골라야 한다.

- **(1) Alt+Tab으로 부르고 싶다** → 위젯을 평범한 앱 창으로 바꾸고(작업표시줄·Alt+Tab 등장), "항상 바탕화면 아래" 정체성은 포기. **이 모드는 지금 코드에 이미 구현돼 있다** — '작업표시줄·트레이 표시'를 켜면 곧바로 됨(지금 켜져 있음). 단 ✕로 트레이에 숨기는 동안에는 OS 규칙상 Alt+Tab에서 빠지고, 트레이 아이콘을 클릭해 다시 띄워야 잡힌다.
- **(2) 바탕화면 상주를 유지하고 싶다** → Alt+Tab 대신 **전역 단축키로 "잠깐 앞으로 소환"**, 또는 **"항상 위"로 늘 보이게** 하는 것이 상용 위젯의 정석.

---

## 1. 배경 / 질문

- 사용자 요구: "트레이 모드일 때 작업표시줄 앱 목록에도 떠서, 다른 앱을 보면서 Alt+Tab으로 전환해 보고 싶다 — 트레이 모드의 원래 목적이 그거 아니냐."
- 현재 코드 상태: 위젯은 `WS_EX_TOOLWINDOW`(작업표시줄·Alt+Tab 의도적 제외) + `HWND_BOTTOM`(항상 최하위)이 기본. 트레이 모드를 켜면(이번에 구현) 일반 앱 창으로 전환됨.

---

## 2. 분석 기준이 된 상용 앱·기술 (실제 조사 대상 · 출처)

| 대상 | 작업표시줄 | Alt+Tab | Z순서 | "다른 앱 쓰는 중 불러오기" | 출처 |
|---|---|---|---|---|---|
| **Windows 11 위젯 보드** | 왼쪽 끝 단일 '버튼'만 | ✗ (오버레이 패널) | 임시 오버레이 | ✗ — Win+W 핫키 / 버튼 클릭 / 핀 | learn.microsoft.com widgets, windowscentral |
| **구버전 데스크톱 가젯** | ✗ | ✗ | 바탕화면 플로팅(+'항상 위' 옵션) | ✗ — Win+G, '항상 위'로 상시 표시 | Wikipedia Desktop Gadgets, sevenforums |
| **Rainmeter 스킨** | ✗ | ✗ | **5단계 사용자 선택**(Stay Topmost/Topmost/Normal/Bottom/On Desktop) | ✗ — 'Topmost'로 상시, 또는 스킨 핫키 | docs.rainmeter.net (Position) |
| **Slack / Discord** | 보일 때 O, 트레이 숨김 시 ✗ | 보일 때 O | 일반 | O (보이는 동안). Discord는 '–'=작업표시줄/'✕'=트레이 분리 | learn.microsoft.com tray, devblogs |
| **Telegram / Spotify** | 트레이 숨김형(닫기=트레이 옵션) | 숨김 중 ✗ | 일반 | 복귀 후 O | 앱 도움말 |
| **Sticky Notes / OneNote / Calculator** | 일반/선택 | 일반/선택 | **'항상 위' 토글**(Keep on top) | 'Alt+Tab'이 아니라 '늘 보이게'로 해결 | Calculator Alt+↑, OneNote Always on Top |
| **Win32 규범 (핵심 레버)** | `WS_EX_APPWINDOW`=강제 노출 / `WS_EX_TOOLWINDOW`=제외 | 두 스타일이 작업표시줄·Alt+Tab을 **함께** 제어 | `HWND_TOPMOST`/일반/`HWND_BOTTOM` = **독립 축** | 노출=TOOLWINDOW 제거+APPWINDOW. 단 '숨김(WS_VISIBLE 제거)' 중엔 무조건 제외 | The Old New Thing, MS 확장 스타일 문서 |

**조사한 바탕화면 상주 위젯(Win11 위젯·가젯·Rainmeter)은 전부 작업표시줄/Alt+Tab에 독립 항목으로 안 들어간다.** 이들은 '전환 가능한 앱 창'이 아니라 '바탕화면에 상주하는 표면'이기 때문이다. 이들의 표준 호출법은 Alt+Tab이 아니라 **① 전용 핫키(Win+W/Win+G) ② '항상 위'로 상시 표시 ③ Win+D로 드러내기**다.

---

## 3. 창 모드 스펙트럼 (사실 정리)

| 모드 | 작업표시줄 | Alt+Tab | Z순서 | 용도 | 대표 |
|---|---|---|---|---|---|
| **트레이 전용(숨김형)** | 숨김 중 ✗ | 숨김 중 ✗ | 보일 때 일반 | 평소 치우고 필요할 때만 꺼내기 | Telegram, Spotify |
| **일반 앱 창 + 트레이(노출형)** | 보일 때 O | 보일 때 O | 일반(덮일 수 있음) | **다른 앱 중 Alt+Tab으로 보기 — 핵심 요구 직결**. 단 바탕화면 상주는 포기 | Slack, Discord, **현재 트레이 ON** |
| **항상 위(Topmost) 위젯** | 선택 | 선택 | 항상 위 | Alt+Tab 없이 '늘 보이게'로 해결 | Calculator/Sticky/OneNote Keep-on-top |
| **바탕화면 상주(항상 아래) — 현행 기본** | ✗ | ✗ | 항상 최하위 | 벽지처럼 늘 깔려 있음. Alt+Tab 호출 구조적 불가 | Rainmeter 'On Desktop', 현재 트레이 OFF |

---

## 4. 우리 위젯에 적용 가능한 선택지

### A. 현행 트레이 토글 그대로 + 안내 명확화 (변경 최소)
- **무엇**: 노출·Z순서·✕동작을 'TrayEnabled' 하나에 묶은 현 구현 유지. 문구만 '바탕화면 위젯 ↔ 앱 창' 모드로 명확화.
- **Alt+Tab**: ✅ 이미 됨(트레이 켜면 됨). 트레이 라벨도 이미 '작업표시줄·트레이 표시 (Alt+Tab 전환, 닫기→트레이)'로 변경 완료.
- **장점**: 추가 작업 거의 없음 / 요구 이미 충족. **단점**: 3~4개 축이 한 토글에 묶여 '바탕화면 상주'와 'Alt+Tab'을 동시에 못 가짐.

### B. 축 분리 — '작업표시줄 표시'와 '트레이 숨김'을 별도 토글로 (권장)
- **무엇**: 한 토글에 묶인 (1)노출 (2)최하위 해제 (3)✕=트레이숨김 (4)트레이 아이콘을 **독립 설정**으로 분리.
- **Alt+Tab**: ✅ 더 유연. '작업표시줄 표시'만 켜면 바탕화면 상주와 무관하게 Alt+Tab 가능. Discord식 '✕=트레이 / 최소화=작업표시줄'도 가능.
- **장점**: 사용자 멘탈모델과 일치, 상용 표준(Slack/Discord)에 부합, 확장 쉬움. **단점**: `SetTrayEnabled` 하나가 4축을 좌우 → 재배선 필요(중간 난이도), 양립 불가 조합(트레이 숨김 중 Alt+Tab)을 UI에 명시해야.

### C. Rainmeter식 Z순서 선택 추가 — 항상 위 / 일반 / 항상 아래(현행)
- **무엇**: B에 더해 Z순서를 3단계로. '넓게 보기'의 일시 topmost 로직을 정식 'always on top'으로 승격 재활용.
- **Alt+Tab**: ✅ '항상 위'를 켜면 Alt+Tab 없이도 늘 보임(흘끗 보기 정답). '항상 아래' 선택 시 Alt+Tab 호출은 포기하는 트레이드오프를 자각하고 선택.
- **장점**: Rainmeter급 자유도, 두 방법(Alt+Tab·항상위) 모두 제공. **단점**: 작업량 최대, '항상 아래 + Alt+Tab'은 재침강 깜빡임 본질적으로 남음(완전 양립 아님).

### D. 전역 핫키 소환 — Notezilla/Win+W식 '앞으로 가져오기'
- **무엇**: 현행(항상 아래+도구창) 유지하되 전역 핫키로 '위젯을 잠깐 앞으로'.
- **Alt+Tab**: ❌ 여전히 불가(도구창 유지). 대신 핫키로 소환.
- **장점**: 바탕화면 상주 정체성 유지하며 '다른 앱 중 보기' 추가, 코드 국소 변경. **단점**: 사용자가 원한 건 Alt+Tab이지 핫키가 아님(직답 아님), 전역 핫키 충돌 가능.

---

## 5. 권고 (강요 아님 · 사용자 판단용)

1. **기본값은 현행 '바탕화면 위젯'(트레이 OFF) 유지** — 제품 정체성('바탕화면 아이콘처럼 상주')이자 첫인상.
2. 그 위에 **옵션 B(축 분리)** 를 우선 — '작업표시줄·Alt+Tab에 표시'를 독립 토글로. 그러면 ① 그대로 두면 바탕화면 위젯, ② 토글만 켜면 Alt+Tab으로 불러보기가 즉시 충족.
3. 여력이 되면 **옵션 C(Z순서: 항상위/일반/항상아래)** 까지 더하면 Rainmeter급 자유도.
4. **현황**: 코드는 이미 '트레이 ON = 일반 앱 창(작업표시줄+Alt+Tab)'으로 동작한다(MainWindow.xaml.cs ApplyWindowMode). 즉 지금도 **트레이 켜면 Alt+Tab으로 볼 수 있다**. 혼란의 원인은 그 토글이 '노출 + ✕=숨김 + 최하위 해제'를 한 묶음으로 묶은 것 → '축 분리'가 핵심.

### 노출하면 좋을 설정 토글 (B+C 기준)
- 작업표시줄/Alt+Tab에 표시 (On/Off) — `APPWINDOW`↔`TOOLWINDOW`
- 창 위치(Z순서): 항상 위 / 일반 / 항상 아래(바탕화면 상주, 현행)
- ✕ 버튼 동작: 트레이로 숨김 / 완전 종료 (Discord식)
- 트레이 아이콘 표시 (On/Off) — 숨긴 뒤 복귀 수단
- (선택) '앞으로 가져오기' 전역 핫키 — '항상 아래' 유지하며 소환

---

## 6. 반드시 알아야 할 한계 (적대적 검증)

- **상호 배타**: "항상 아래"와 "Alt+Tab 호출"은 OS상 동시 불가. 절충('보일 때만 앞, 떠나면 아래')은 **올라왔다 가라앉는 깜빡임**이 불가피.
- **숨김 = 목록 제외**: ✕로 트레이에 숨긴 동안은 `WS_VISIBLE`이 빠져 Alt+Tab/작업표시줄에서 사라지는 게 **정상**(버그 아님). 어떤 상용 앱도 '숨긴 채 작업표시줄 상주'는 제공하지 않음.
- **가상 데스크톱**: Win11 Alt+Tab 기본값은 '현재 데스크톱 창만' 표시 → 위젯을 띄운 데스크톱이 아니면 안 보일 수 있음(OS 설정 종속).
- **Win11 스냅**: 일반 창 모드에선 캡션이 없어(CaptionHeight=0) Win+화살표 스냅 시 위젯이 모니터 절반으로 강제 리사이즈될 수 있음.
- **WPF ShowInTaskbar 토글**: 런타임 전환이 잦아지면 owner 창 재부착/HWND 영향으로 WebView2 재렌더 리스크 → 회귀 테스트 필요.
