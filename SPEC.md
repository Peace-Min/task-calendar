# 수행과제 캘린더 — 요구사항 분석 · 구현 명세 · 자체 검증 루프 (v1, 2026-06-11)

## 1. 환경 제약 분석

| 제약 | 설계 반영 |
|---|---|
| Windows 11, 오프라인 폐쇄망 | 외부 리소스(CDN, 웹폰트, 네트워크 호출) **0개**. 단일 HTML 파일로 완결 → USB 등으로 복사만 하면 실행 가능 |
| 프로토타입 우선 (HTML) | 브라우저(Edge)에서 `file://`로 더블클릭 실행. 설치 불필요 |
| 데이터 영속성 | 프로토타입: 브라우저 localStorage 자동 저장 + **XML 파일 내보내기/가져오기**로 백업·이동 |
| 추후 네이티브 앱 전환 | 데이터 포맷을 XML로 표준화해 두어 WPF / Electron / Tauri 등 어떤 스택으로 가도 그대로 이식 |

## 2. 요구사항 → 기능 매핑

| ID | 사용자 요구사항 | 구현 기능 |
|---|---|---|
| R1 | "수행 과제" 카테고리 등록 | 과제 관리 모달: 과제 생성/수정/삭제, 색상 지정, 설명, 과제별 기록 수 표시 |
| R2 | 날짜별 위젯 버튼 클릭 → 기록 작성 UI | 월 그리드의 날짜 셀 클릭 → 우측 날짜 패널, 더블클릭/＋버튼 → 기록 작성 모달(제목·과제·시간·메모) |
| R3 | 키워드 검색 → 어떤 날에 입력했는지 표시 | 키워드 검색: 제목+메모+과제명 대상, **날짜별 그룹핑** + 매칭 부분 하이라이트, 클릭 시 해당 날짜로 이동 |
| R4 | 과제 카테고리 검색 → 해당 과제 기록 전부 보기 | 과제별 모아보기: 시간순 전체 기록 + 요약(총 건수, 첫/마지막 기록일). 캘린더 상단 과제 필터 칩도 제공 |
| R5 | 기계가 읽기 쉬운 포맷(XML류) | §3의 XML 스키마. 내보내기/가져오기(병합·교체), 왕복(round-trip) 무결성 보장 |
| R6 | 범용 캘린더 기본 기능 | 월 이동/오늘/연·월 점프, 종일·시간 일정, 색상 표시, 양력 공휴일, 드래그로 날짜 이동, 키보드 단축키, "+N개" 오버플로 처리 |
| R7 | 자체 기능·UI/UX 검증 루프 | §4 검증 루프 — 구현 후 자동 실행 |
| R8 | HTML 프로토타입 우선 | 본 산출물 `task-calendar-prototype.html` |

### 프로토타입 범위에서 제외(네이티브 단계 백로그)
주간/일간 뷰, 알림(폐쇄망 데스크톱 알림은 네이티브에서), 반복 일정, 음력 공휴일(설·추석 — 음력 변환 필요), 파일 직접 자동저장(브라우저 보안 제약 → 네이티브에서는 XML 파일을 직접 읽고 씀), 다중 사용자.

## 3. XML 데이터 스키마 (`taskCalendar` v1)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<taskCalendar version="1" generator="TaskCalendar-Prototype" exportedAt="2026-06-11T03:00:00.000Z">
  <categories>
    <category id="c-..." color="#3e5be0" createdAt="...">
      <name>보고서 작성</name>
      <description>주간/월간 보고서 작업</description>
    </category>
  </categories>
  <entries>
    <entry id="e-..." date="2026-06-11" categoryId="c-..." allDay="false"
           startTime="10:00" endTime="11:30" createdAt="..." updatedAt="...">
      <title>프로토타입 요구사항 정리</title>
      <memo>상세 내용 (특수문자 &amp;, &lt; 및 줄바꿈 모두 안전하게 이스케이프)</memo>
    </entry>
  </entries>
</taskCalendar>
```

- 식별자/날짜/시간/플래그는 **속성**, 사람이 쓰는 자유 텍스트는 **자식 요소** — 파서가 다루기 쉽고 확장 시 요소 추가만 하면 됨.
- 날짜 `YYYY-MM-DD`, 시간 `HH:mm`(24h), 타임스탬프 ISO 8601 — 어떤 언어의 표준 라이브러리로도 파싱 가능.
- 가져오기 시 `version` 확인, 날짜 형식 검증, ID 기준 중복 제거(병합 모드).

## 4. 자체 검증 루프 (구현 직후 실행)

**종료 조건: L1~L5 전부 통과 & 콘솔 에러 0 & 확인된 결함 0. 결함 발견 시 수정 → 해당 레벨부터 재실행.**

- **L1 정적 검증** — 외부 리소스 참조 0개(http/https URL 부재), 모든 사용자 입력이 HTML 이스케이프를 거치는지, 날짜 연산이 타임존 안전(`toISOString` 미사용)인지 점검.
- **L2 기능 검증** — 실제 브라우저를 자동 조작(클릭/입력/스크린샷)하며 아래 체크리스트 수행, 매 단계 콘솔 에러 0 확인.
- **L3 데이터 검증** — XML 내보내기→파싱→재가져오기 왕복 비교(필드 단위 일치), 특수문자(`< > & " '` 줄바꿈) 포함 데이터로 반복, 새로고침 후 데이터 유지 확인.
- **L4 UI/UX 검증** — 기본(1280px)·축소(800px) 해상도 스크린샷 검토, 빈 상태/긴 제목/기록 5개 이상인 날의 표시 확인.
- **L5 다중 관점 코드리뷰** — 5개 독립 관점(JS 로직·날짜 연산 / XML·데이터 무결성 / 명세 충족도 / 오프라인·Edge 호환 / 입력 검증·XSS)으로 병렬 리뷰 → 발견 항목마다 교차(반박) 검증 → 확인된 결함만 수정 → L2 재실행.

### L2 기능 체크리스트

| # | 항목 | 통과 기준 |
|---|---|---|
| F1 | 초기 렌더 | 이번 달 그리드, 오늘 강조, 콘솔 에러 0 |
| F2 | 월 이동 | ◀/▶/오늘 버튼, ←/→/Home 단축키, 연·월 점프 |
| F3 | 날짜 선택 | 셀 클릭 → 우측 패널에 해당 날짜 기록 표시 |
| F4 | 기록 추가 | 모달 입력 → 저장 → 셀 칩 + 패널 카드 반영 |
| F5 | 기록 수정/삭제 | 값 변경 반영, 삭제 시 확인 후 제거 |
| F6 | 종일/시간 전환 | 종일 체크 시 시간 입력 비활성, 표시 "종일" |
| F7 | 시간 유효성 | 종료<시작 입력 시 저장 차단 + 경고 |
| F8 | 제목 필수 | 빈 제목 저장 차단 |
| F9 | 과제 CRUD | 생성(색상)/수정/삭제, 삭제 시 소속 기록 미분류 처리 |
| F10 | 과제 색상 반영 | 칩/카드/배지에 과제 색 적용 |
| F11 | 키워드 검색 | 날짜별 그룹 + 하이라이트 + 건수/일수 요약 |
| F12 | 검색 → 이동 | 결과 클릭 시 해당 월·날짜로 이동, 대상 기록 강조 |
| F13 | 과제별 모아보기 | 해당 과제 전체 기록 시간순 + 요약 통계 |
| F14 | 과제 필터 칩 | 칩 클릭 시 캘린더에 해당 과제만 표시 |
| F15 | XML 내보내기 | 파일 다운로드, 스키마 §3 준수 |
| F16 | XML 가져오기 | 병합(중복 ID 제외)/교체 선택 동작 |
| F17 | 왕복 무결성 | 내보내기→가져오기 후 데이터 동일 |
| F18 | 드래그 이동 | 칩을 다른 날짜 셀로 드래그 → 날짜 변경 |
| F19 | 새로고침 유지 | F5 후 데이터 그대로 |
| F20 | 데이터 초기화 | 확인 후 전체 삭제 |

## 5. 검증 결과 기록 (2026-06-11 실행)

### L1 정적 검증 — ✅ 통과
- 외부 리소스 참조(`http/https`, `src=`, `url()`, `@import`) **0건** — 폐쇄망 단일 파일 조건 충족.

### L2 기능 검증 — ✅ F1~F20 전체 통과 (브라우저 자동 조작, 콘솔 에러 0)
- F1 초기 렌더: 42셀, 오늘 강조, 시드 데이터(과제 2·기록 3) ✅
- F2 월 이동(버튼/단축키 ←·Home/연월 점프 2025-12 → 성탄절 표시) ✅
- F3 날짜 선택 → 패널 동기화 ✅
- F4 기록 추가(특수문자 `< > & "` + 줄바꿈 메모 포함) ✅
- F5 수정(칩 클릭 → 폼 프리필 → 저장 반영) / 삭제 ✅
- F6 종일 토글 시 시간 입력 비활성 ✅
- F7 종료<시작 저장 차단 + 경고 토스트 ✅
- F8 빈 제목 저장 차단 ✅
- F9 과제 추가/수정/삭제(삭제 시 소속 기록 미분류 전환) ✅
- F10 과제 색상이 칩 배경/테두리에 반영 ✅
- F11 키워드 검색 '검증': 2건·2일, 날짜 그룹 + `<mark>` 하이라이트 3곳 ✅
- F12 결과 클릭 → 모달 닫힘 + 해당 날짜 이동 + 대상 카드 플래시 ✅
- F13 과제별 모아보기: 총 3건 + 기간 요약 시간순 ✅
- F14 과제 필터 칩: 5칩 → 1칩 필터링/해제 ✅
- F15 XML 스키마: 헤더/루트/엔트리 수/이스케이프(`&lt;특수문자&gt; &amp;`) ✅
- F16 가져오기 교체(동일 복원)·병합(삭제 후 복원, 중복 ID 제외) ✅
- F17 왕복 무결성 `xmlRoundTrip → ok:true` ✅
- F18 칩 드래그 6/16→6/18 날짜 이동 ✅
- F19 새로고침 후 데이터 유지(과제 2·기록 5) ✅
- F20 전체 초기화 → 0·0, 빈 상태 안내 표시 ✅

### L3 데이터 검증 — ✅ 통과
- 특수문자·줄바꿈 포함 데이터의 XML 왕복 비교 일치, localStorage 영속성 확인.

### L4 UI/UX 검증 — ✅ 통과
- 1280px: 오늘 강조·공휴일·색상·필터 칩·날짜 패널 정상.
- 800px: 상단 바 줄바꿈, 날짜 패널이 캘린더 하단으로 스택 — 깨짐 없음.

### L5 다중 관점 코드리뷰 — ✅ 완료 (89개 에이전트, 5관점 × 3인 반박검증)
반박 다수결을 통과한 **확인 결함 3건**과 고칠 가치 있는 경미 항목을 수정함.

| # | 결함 | 심각도 | 수정 |
|---|---|---|---|
| 1 | XML 가져오기 `id`/`startTime`/`endTime`이 무검증·무이스케이프로 `innerHTML`에 삽입 → 저장형 XSS | High | `fromXML`에 `safeId`(안전문자만)·`validTime`(HH:mm) 검증 추가 + 모든 렌더 사이트(`chipHtml`/`cardHtml`/`sRowHtml`/`renderFilterbar`/`renderCatModal`/`fillCatSelect`)에서 `id`·`time`·`color` 이스케이프(이중 방어). flash 셀렉터 `CSS.escape` 적용 |
| 2 | 가져오기 날짜를 형식만 검사 → `2026-02-31` 등 비실재 날짜 통과(유령 기록) | Medium | `isRealDate`로 실재 날짜만 통과(Date 왕복 비교) |
| 3 | 검색 결과 클릭 시 활성 과제 필터가 대상 기록을 가려 강조 미표시 | Medium | `goto`에서 필터 해제 후 이동, `saveEntryFromForm`도 필터 불일치 시 해제 |

추가 보강(경미·명세 항목): `version` 속성 경고, 종일 기록의 잔여 시간 정규화, 병합/교체 시 **파일 내부 중복 ID 제거**, `load()`에서 손상 데이터 문자열 정규화(검색 크래시 방지), 종료시간만 입력 시 `~HH:mm` 표시, 드래그 `dragend` 잔상 정리.

### 재검증(수정 후) — ✅ 전부 통과, 콘솔 에러 0
- XSS 재현 시도: 주입 `<img>` 0개, `onerror` 실행 0회, localStorage 저장 0, 검색결과 0 — **완전 차단**.
- 유령 날짜: `2026-02-31`·`2026-13-05` 거부, 종일 기록 시간 비워짐 확인.
- 검색→이동: 필터 자동 해제 + 대상 카드 플래시 표시 확인.
- F1~F20 전체 회귀 재실행 통과(특수문자 메모 왕복·중복ID 병합 1건만·드래그·새로고침 유지·초기화 포함).

**검증 루프 종료 조건 충족: L1~L5 통과, 콘솔 에러 0, 확인된 결함 0.**

### 좌/우 너비 조절 분할바 (사용자 요청, 프로토타입+위젯 공통)
- 좌측 캘린더(.cal, flex:1)와 우측 기록패널(.day-panel) 사이에 세로 분할바(`#splitter`) 추가. 드래그 시 `--dpw` CSS 변수로 day-panel 너비 조절, `localStorage('taskCalendar.panelW')`에 저장(UI 환경설정 — XML 데이터와 별개, WebView2 프로필에도 영속).
- 클램프: 패널 최소 240px, 캘린더 최소 ~380px(패널 ≤ main폭-380).
- 반응형 브레이크포인트 920→**760px**로 하향(좌우 배치가 더 작은 폭에서도 나오도록). 그 미만은 세로 스택 + 분할바 숨김.
- 검증: 브라우저 프리뷰에서 드래그 340→464px·클램프(캘린더 최소 보호/패널 최소 240)·새로고침 후 300px 복원·좁은 폭 분할바 숨김+스택 확인(콘솔 0). 위젯(820×560) 캡처로 좌우 배치+분할바 렌더 확인.

---

## 6. 2단계 — 데스크톱 위젯 (2026-06-11)

사용자 확정 요구: 실행형 앱이 아니라 **바탕화면 상주 위젯**(로그인 시 자동 상주, 아이콘처럼 데스크톱 레이어에 고정, 단 조작 가능). 환경: 오프라인 폐쇄망 Win11, .NET SDK 9.0.315 + WindowsDesktop 9.0.17, WebView2 런타임 149.x, WebView2 NuGet 1.0.3179.45 로컬 캐시 보유.

**구조**: WPF 껍데기 + WebView2가 검증 완료 HTML을 임베드(`widget/`). 데이터는 localStorage 대신 호스트 브리지를 통해 **`%APPDATA%\TaskCalendar\data.xml`**(taskCalendar XML v1, HTML 내보내기와 호환)에 저장. 창은 테두리 없는 최하위(bottom-most) 도구 창 + 작업표시줄/Alt+Tab 제외 + 로그인 자동시작(HKCU\Run).

**바탕화면 부착(사용자 선택: "단단히 고정 + 조작 가능")**: 창을 **바탕화면(Progman)의 자식 창 + 아이콘 레이어 위(HWND_TOP)**로 SetParent 부착. → Win+D에도 잔존, 아이콘 위라 클릭/입력 가능, 일반 앱 뒤로 깔림. 기본 이동 잠금(⚙에서 고정/플로팅 토글). 진짜 벽지(WorkerW, 아이콘 뒤) 매립은 아이콘 레이어가 클릭을 가로채 조작 불가라 배제.

**검증(2026-06-11)**:
- 오프라인 빌드 성공(오류 0, 경고 0) — RuntimeFrameworkVersion 9.0.17 고정으로 미설치 패치 NuGet 다운로드 회피.
- 종단간 스모크 테스트: exe 실행 → 크래시 없음 → `data.xml` 자동 생성(시드 3건, 정상 XML) → WebView2 로드·`ready` 핸드셰이크·호스트 파일 기록 전 파이프라인 동작 확인. 자동시작 레지스트리 등록 확인.
- **바탕화면 부착 OS 레벨 확인**: 위젯 실행 후 Progman의 자식 창 열거 결과 `HwndWrapper[TaskCalendarWidget;...]` 존재 → 데스크톱 자식으로 부착 성공. data.xml도 임베드 상태에서 정상.
- **미검증(육안 필요)**: 임베드 상태의 렌더링·클릭·한글 IME 입력은 스크린샷 불가 → 사용자 실행 확인 필요(이상 시 ⚙에서 플로팅으로 폴백 가능).

### 하단 검은 띠 수정 (사용자 스크린샷 피드백)
- 증상: 창 하단에 검은 띠.
- **1차 가설(오답)**: DPI 불일치 추정 → PerMonitorV2 매니페스트 + `web.DefaultBackgroundColor=White` 적용했으나 띠 그대로. (두 변경은 무해해 유지.)
- **측정 진단**: 임시 로깅으로 실제 치수 확보 → DPI=1.0(배율 무관), WPF 내부는 완전히 채워짐(web+타이틀+보더=클라이언트). 단 `window(px)=380x470` vs `client(px)=366x456` — **클라이언트가 창보다 14px 작음.**
- **진짜 원인**: `WindowStyle="None"` + 리사이즈 가능 창이 남기는 **비클라이언트 리사이즈 테두리**가 검게 렌더(테두리 없는 리사이즈 WPF 창의 알려진 현상). 흰 푸터와 맞닿는 하단에서 가장 도드라짐.
- **수정**: 코드비하인드에서 `WindowChrome`(CaptionHeight=0, GlassFrameThickness=0, ResizeBorderThickness=6) 적용 → 비클라이언트 영역 회수, 클라이언트=창 전체. 재측정 결과 `client(px)=380x470 == window(px)=380x470`(비클라이언트 0)로 **객관적 제거 확인**. 리사이즈는 가장자리 6px로 유지. 진단 로깅은 제거함.
- 교훈: 추측(DPI) 대신 측정(client vs window 치수)이 원인을 정확히 짚음.

### 검은 띠 — 진짜 최종 원인 & 해결 (자체 화면 캡처 검증)
- WindowChrome로 좌/우는 사라졌으나 사용자 피드백상 **상/하 검은 띠 잔존**.
- **자체 검증 도입**: PowerShell `System.Drawing` + `PrintWindow`로 위젯 창을 직접 화면 캡처 → PNG를 Read로 확인(네이티브 창도 스스로 검증 가능해짐).
- 캡처 분석: 임베드 상태에서 **WebView2 영역(달력)은 정상, WPF가 그리는 부분(파란 타이틀바·테두리)만 검게 렌더**. 플로팅에서는 WPF 타이틀바 정상.
- **진짜 최종 원인**: WPF 창을 Progman 자식으로 `SetParent`하면 **WPF 자체 렌더링(DWM 합성)이 깨짐** → WPF가 그리는 영역이 검게 남음(WebView2는 독립 HWND라 영향 없음).
- **해결**: WPF 크롬 제거(`<wv2:WebView2>`가 창 100% 채움, `ResizeMode=NoResize`) + 타이틀바 컨트롤(이동/⚙/✕/고정)을 **HTML 호스트바**로 이동하고 `postMessage`로 호스트에 전달. 크기는 ⚙ 프리셋으로(가장자리 리사이즈는 WebView2가 덮어 불가).
- **검증**: 고정·기본(380×470)·큰 창(900×640) 모두 PrintWindow 캡처로 **검은 띠 소멸 확인**. 빌드 0 오류/경고.

### 드래그/리사이즈 복구 (자체 마우스 시뮬레이션 검증)
- 증상: WebView2 100% 채움으로 바꾼 뒤 **이동 안 됨 + 가장자리 리사이즈 없음**(NoResize + 잠금).
- 방식: 타이틀바를 없앴으므로 이동/리사이즈를 **HTML 포인터 이벤트 → postMessage → 호스트 `SetWindowPos`**(물리 px, devicePixelRatio 보정)로 구현. 호스트바 드래그=이동, 우하단 그립=리사이즈. 부착/플로팅 모두 동작.
- **자체 검증(마우스 시뮬레이션)**: PowerShell `mouse_event`+`SetCursorPos`로 실제 드래그를 흉내내고 `GetWindowRect`로 이동/크기 변화를 측정.
  - 이동: 호스트바 드래그 → 창이 정확히 +130,+90 / +120,+70 이동 ✅
  - 리사이즈(넓은 창 960): +150,+100 정확 ✅
- **막혔던 원인 2건(측정으로 규명)**:
  1. `ResizeMode=NoResize`가 크기를 고정(min=max) → `SetWindowPos` 리사이즈를 되돌림. → `CanResize` + `WindowChrome`(비클라이언트 0)로 변경.
  2. 좁은 폭(≤920) 반응형 `body{overflow:auto}` + 큰 min-height가 **세로 스크롤바**를 만들어 그립을 코너에서 15px 밀어냄(클릭이 스크롤바에 맞음). 호스트 ExecuteScript로 그립 `fromRight=15` 측정해 규명. → `body.host{overflow:hidden}` + 좁은 폭 레이아웃 재정의(날짜패널 내부 스크롤)로 해결.
- 재검증: 좁은 창(430×540)에서도 리사이즈 +120,+90 정확 ✅, 날짜패널 표시 + 그립 코너 확인.

### 부착 시 이동(드래그) 잠금 (사용자 요청)
- 요구: 바탕화면 부착 상태에서는 이동(드래그)을 막아야 함(제자리 고정). 리사이즈는 유지(좌상단 고정·크기만 변경이라 위치 고정과 무충돌).
- 구현: HTML에서 `__pinned`일 때 드래그 제스처 시작 안 함(+ 이동 커서 제거) / 호스트도 `_settings.Pinned`면 drag* 메시지 무시(이중 차단). 리사이즈는 양쪽 모드 모두 허용.
- **자체 검증(Win+D로 부착 위젯 노출 후 마우스 시뮬레이션)**: 부착 상태에서 드래그 시도 → 이동량 0,0(잠금) ✅, 같은 위젯에서 리사이즈 → +120,+80 정확 ✅(리사이즈가 먹혔다는 건 클릭이 위젯에 명중했다는 증거 → 드래그 0,0은 빗나감이 아니라 잠금). 부착 위젯이 Win+D 후에도 표시됨도 함께 확인 ✅.
