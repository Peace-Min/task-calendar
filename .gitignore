# ── .NET 빌드/배포 산출물 (소스만 버전관리, 결과물은 제외) ──
[Bb]in/
[Oo]bj/
dist/
publish/
*.user
*.suo
.vs/

# ── 도구/에디터 ──
.claude/
.idea/
*.tmp

# ── OS ──
Thumbs.db
desktop.ini
.DS_Store

# 참고: 실제 캘린더 데이터·위젯 설정은 %APPDATA%\TaskCalendar\ 에 저장되며
#       저장소(소스)에는 포함되지 않습니다. 백업은 앱의 [데이터 → XML 내보내기]를 사용하세요.

# 진단용 임시 위젯 캡처
_widget_capture.png

# 하네스 변이 시험(tests/harness-selftest.test.mjs)이 만드는 임시 사본 — finally 에서 지우지만
#   강제 종료 시 남을 수 있다. 러너는 *.test.mjs 만 줍기 때문에 실행에는 영향이 없다.
tests/.harness-mutant-*
