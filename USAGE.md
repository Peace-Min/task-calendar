# 📖 사용 설명서 — 수행과제 캘린더

> 처음 쓰는 분을 위한 기능별 사용법 + 키보드 단축키 + 문제 해결. 빌드/배포는 [DEPLOY.md](DEPLOY.md), 내보내기 XML 포맷은 [SPEC.md](SPEC.md) §3을 보세요.
>
> **v0.18.0부터 기록은 이 PC가 아니라 사내 서버 DB에 저장됩니다.** 어느 자리에서 로그인해도 같은 캘린더가 뜨는 대신, **서버에 연결되어야 동작**합니다. 옛 버전에서 쓰던 기록은 [13. 데이터·백업·이관](#13-데이터백업이관)의 절차로 **한 번만** 옮기면 됩니다.

<img src="assets/screenshot.png" alt="수행과제 캘린더 화면" width="820">

---

## 목차
1. [시작하기](#1-시작하기)
2. [화면 구성](#2-화면-구성)
3. [일정·할 일 등록 (빠른 캡처)](#3-일정할-일-등록-빠른-캡처)
4. [수정·삭제](#4-수정삭제)
5. [기간·반복 일정](#5-기간반복-일정)
6. [할 일(TODO)](#6-할-일todo)
7. [Git 커밋 → 작업일지](#7-git-커밋--작업일지)
8. [보고서 만들기](#8-보고서-만들기)
9. [검색·과제 필터](#9-검색과제-필터)
10. [창 모드·트레이·크기 조절](#10-창-모드트레이크기-조절)
11. [테마(다크 모드)](#11-테마다크-모드)
12. [키보드 단축키](#12-키보드-단축키)
13. [데이터·백업·이관](#13-데이터백업이관)
14. [문제 해결(FAQ)](#14-문제-해결faq)

---

## 1. 시작하기

- **위젯(권장)**: 배포된 `수행과제캘린더.exe`를 실행하면 바탕화면에 위젯이 뜹니다. `Win+D`로 바탕화면을 봐도 함께 보입니다.
- **회사 계정으로 로그인**: 처음 켜면 로그인 화면이 뜹니다. **주간보고(netcus)에 쓰는 그 계정**입니다 — 회사 **이메일 주소가 아닙니다**(`phmin@netcus.com`이면 `phmin`). 한 번 로그인하면 세션이 유지됩니다.
- **브라우저(체험)**: `task-calendar-prototype.html`을 더블클릭하면 Edge에서 캘린더·보고서 초안은 그대로 동작합니다. 단 **서버 DB에 붙지 않아** 데이터는 그 브라우저의 localStorage에만 남고 위젯과 공유되지 않으며, 로그인·커밋 수집·회사 보고 전송·알림은 없습니다.
- 첫 실행 때 **자동 시작(로그인 시 실행)** 여부를 1회 물어봅니다. 나중에 ⚙(설정)에서 변경할 수 있습니다.
- **과제(카테고리) 먼저 만들기**: 상단바 `🏷️ 과제 관리`(좁은 창에서는 `⋯ → 과제 관리`)에서 "보고서 작성", "시스템 점검"처럼 수행 과제를 만들고 색을 지정하세요. (Git 연동 시 저장소 경로도 여기서.) 사업부 **공식 과제**는 서버 DB의 마스터에서 골라 씁니다.
- **옛 버전에서 넘어왔다면**: 캘린더가 비어 보이면 그건 데이터가 사라진 게 아니라 **아직 안 옮긴 것**입니다 — [13. 데이터·백업·이관](#13-데이터백업이관)을 보세요.

## 2. 화면 구성

| 영역 | 설명 |
|---|---|
| **상단바(좌)** | `◀ 오늘 ▶` 월 이동 · 연·월 제목(클릭 시 점프) |
| **상단바(우)** | `🔍 검색` · `🏷️ 과제 관리` · `🐞 문의`(버그·개선 접수처) · `👤 사용자 정보`(신원·권한·로그아웃) · `🎨 모양`(테마·위젯 투명도) · `? 도움말` · `⋯ 더보기`(설정·업데이트 확인·회의실 관리·XML 내보내기/가져오기·과제 데이터 내보내기·전체 초기화) · `📄 보고서` · `＋ 새 기록` |
| **과제 필터 바** | 과제별 보기 토글. "전체"가 기본 |
| **월 그리드(좌)** | 주말 옅은 틴트, 오늘은 얇은 링, 선택 날짜는 강조. 일정=채운 막대 / 할 일=속 빈 링 칩 / 시간 일정=색점+시각 |
| **우측 패널** | `[일정 상세 | 할 일 | 커밋 내역]` 탭. 셋 다 **선택한 날짜**를 따라갑니다 |
| **분할바** | 좌/우 너비 드래그 조절 (좁은 폭에선 우측이 하단 시트로) |

> 창이 좁아지면 `검색`·`과제 관리`·`문의`·`사용자 정보`·`보고서`가 `⋯ 더보기` 안으로 접힙니다 — 사라진 게 아닙니다.
> 화면 위에 **`DB 연결됨 — 변경은 서버에 저장됩니다`** 같은 띠가 보이면, 지금 무엇을 보고 있고 저장이 되는 상태인지 알려 주는 출처 배지입니다.

## 3. 일정·할 일 등록 (빠른 캡처)

모든 등록은 **빠른 캡처** 한 곳으로 모입니다. 여는 방법:
- 상단 **＋ 새 기록**
- 날짜 칸 **더블클릭**
- 우측 패널 **＋ 추가** / **＋ 할 일 추가**

빠른 캡처에서:
1. 상단 토글로 **일정 / 할 일** 선택
2. 제목 입력
3. 날짜·과제 지정
4. 필요하면 **옵션 더보기**로 종일/시간·반복·기간·메모(일정) 또는 중요·기간·메모(할 일)
5. **추가**

> 시간을 넣으려면 "종일" 체크를 끄면 시작/종료 시간 칸이 나타납니다.

## 4. 수정·삭제

- **수정**: 캘린더의 일정 칩을 클릭하거나, 우측 카드의 **수정**. 단순 일정은 빠른 캡처(편집 모드)로 열리고, **자세히 편집**을 누르면 전체 편집기(반복 간격·횟수, Git 등)로 넘어갑니다(입력값 보존).
- **삭제**: 카드의 **삭제**. 삭제 후 토스트의 **실행 취소**로 되돌릴 수 있습니다.
- **반복 일정 삭제**: "이 날짜만"(건너뛰기) 또는 "전체 반복" 선택.

## 5. 기간·반복 일정

- **기간(여러 날)**: 옵션 더보기 → "여러 날에 걸침" 체크 → 종료일. 캘린더에 **연속 막대**로 표시되고, 겹치는 막대는 서로 다른 줄(레인)에 고정됩니다.
- **반복**: 옵션 더보기 → 반복(매주/매월). 간격(격주 등)·종료(횟수/날짜)는 **자세히 편집**에서.
- 기간 일정은 막대를 드래그하면 시작·종료가 함께 이동(길이 보존)합니다.

## 6. 할 일(TODO)

- 우측 **할 일** 탭: 선택한 날짜의 [이 날 / 기한 없음 / 이 날 완료] 구획.
- 체크박스로 완료 토글(완료는 취소선 + "완료" 구획으로 이동), **별표**로 중요 표시, **중요만** 버튼으로 필터.
- 제목을 클릭하면 인라인 편집(텍스트·기한·과제·중요).
- 기한이 있는 할 일은 **캘린더에 속 빈 링 칩**으로 표시되어 일정(채운 막대)과 모양으로 구분됩니다(색맹 안전).

## 7. Git 커밋 → 작업일지

> 위젯 모드 전용 (PC에 git 설치 + 과제에 로컬 저장소 경로 연결 필요).

1. `🏷️ 과제 관리`에서 과제에 **Git(또는 SVN) 저장소 경로**와 **내 작성자(author)**를 연결.
2. 우측 **커밋 내역** 탭의 **이 날 커밋** — 그날 내 커밋을 끌어와 작업일지(`source=git`)로 등록.
3. **기간 일괄 불러오기** — 주말에 그 주 전체를 모든(또는 특정) 과제에서 한 번에 등록.
4. 작업일지는 일반 일정과 달리 캘린더 셀에는 숨기고(작업일지 필터 켤 때만 노출), 패널·보고서에는 항상 보입니다.

> **저장소 경로만은 서버에 올라가지 않습니다** — PC마다 경로가 다르기 때문입니다(`D:\repos\...`가 옆자리엔 없거나, 더 나쁘게는 **다른 저장소**일 수 있습니다). 이 PC의 `repo-paths.json`에 남으므로, **자리를 옮기면 과제 관리에서 경로를 다시 연결**해야 커밋을 끌어옵니다. 커밋 내역 자체는 서버에 저장되어 어디서든 보입니다.

## 8. 보고서 만들기

상단바 **📄 보고서**(아주 좁은 창에서는 `⋯ → 보고서 초안`) → **일간 작성 / 주간 작성 / 기간 취합** 탭 → 기간 프리셋(오늘/이번주/지난주/이번달/지난달) 또는 직접 지정 → 과제별·날짜별 **Markdown/텍스트 초안** 생성 → **복사** 또는 파일 저장. 그룹웨어/HWP 주간보고에 붙여넣기 좋게 정리됩니다.

- 미리보기에서 커밋·할 일·일정 항목을 **바로 편집**할 수 있습니다(원본이 함께 고쳐집니다 — 일간 탭에서만).
- 일간 탭의 **📤 일간보고 전송**, 주간 탭의 **📤 주간보고 작성**이 사내 보고 시스템(netcus)에 자동으로 씁니다. **보낸 내용은 서버 DB에도 기록**되어 나중에 확인할 수 있습니다.

## 9. 검색·과제 필터

- **검색**(상단 돋보기): 제목·메모·과제로 전역 검색, 날짜별 그룹 + 하이라이트. 클릭하면 그 날짜로 이동.
- **과제 필터 바**: 특정 과제만 캘린더에 표시.

## 10. 창 모드·트레이·크기 조절

- **창 모드**: `⚙ → 트레이 아이콘 사용`
  - **켜기** = 일반 앱 창(작업표시줄·`Alt+Tab`에 표시, ✕ = 트레이로 숨김).
  - **끄기** = 바탕화면 최하위 위젯(작업표시줄 제외, 항상 바탕화면 레이어).
- **넓게 보기**: 호스트바 `🔳` — 일시적으로 크게·앞으로(다시 누르면 복귀).
- **이동/크기**: 📌(고정) 해제 후 상단 바 드래그로 이동, 가장자리·모서리(8방향) 드래그로 크기 조절. ⚙에서 크기 프리셋·모니터 이동.

## 11. 테마(다크 모드)

상단바 **🎨(모양)** 메뉴에서 고릅니다 — **시스템 · 라이트 · 다크 · 포레스트 · 세피아 · 고대비** 6종. 기본값 **시스템**은 Windows 테마(라이트/다크)를 따라 자동 전환합니다. 모든 아이콘이 단색(currentColor)이라 어느 테마에서도 자연스럽게 보입니다.

같은 메뉴 아래에 **위젯 투명도** 슬라이더가 있습니다(위젯 모드 전용). 반투명으로 해도 모달·토스트·알림 창은 불투명하게 유지됩니다.

## 12. 키보드 단축키

마우스 없이 전체 조작이 가능합니다.

| 상황 | 키 | 동작 |
|---|---|---|
| 월 그리드(셀 포커스) | `←` / `→` | 하루 이전/다음 |
| | `↑` / `↓` | 한 주 이전/다음 |
| | `PageUp` / `PageDown` | 한 달 이전/다음 |
| | `Home` | 오늘로 |
| | `Enter` / `Space` | 그 날짜 선택 |
| 전역(그리드 밖) | `←` / `→` · `PageUp` / `PageDown` | 월 이동 |
| | `Home` | 오늘 |
| 우측 탭 | `←` / `→` / `Home` / `End` | 탭 이동(자동 활성화) |
| 모달 | `Tab` / `Shift+Tab` | 포커스 순환(트랩) |
| | `Esc` | 닫기 (모달→메뉴→하단시트→넓게보기 순) |
| 입력 | `Enter` | 빠른 캡처/인라인 편집 저장 |

> 모달을 닫으면 포커스가 그것을 연 버튼으로 자동 복원됩니다. 접근성: WCAG AA 대비, 가시 포커스 링, 화면 낭독기용 ARIA, `prefers-reduced-motion` 존중.

## 13. 데이터·백업·이관

### 어디에 저장되나

- **일정·할 일·과제·회의실·공수·근태·커밋·보고 기록** — **사내 서버 MySQL**(v0.18.0부터). 어느 자리 PC에서 로그인해도 같은 데이터가 뜹니다. 백업은 서버에서 관리자가 주간 단위로 받습니다(사용자가 챙길 일 없음).
- **이 PC에만 남는 것** — 창 위치·크기(`widget.settings.json`), 알림 확인 이력(`reminders.json`), **과제별 저장소 경로**(`repo-paths.json`), 로그인 세션·회사 보고 자격증명(암호화). 경로는 `%APPDATA%\TaskCalendar\`.
- **로컬 캐시는 없습니다.** 서버에 못 붙으면 기록이 표시되지 않습니다 — 잘못된 옛 데이터를 보여 주는 것보다 안 보여 주는 쪽을 택했습니다.

### 옛 버전 기록 옮기기 (한 번만)

v0.17.1까지는 `%APPDATA%\TaskCalendar\data.xml`에 저장했습니다. **자동으로 옮기지 않습니다** — 확인하고 직접 올리시라고 이렇게 뒀습니다.

1. 업데이트 후 캘린더가 비어 있으면 화면 위에 **「이전 버전에서 쓰던 기록이 있다면 XML 파일에서 가져올 수 있습니다」** 안내(**미이관 표시등**)가 뜹니다.
2. 그 옆의 **XML 가져오기…** 를 누르면 파일창이 **데이터 폴더에서 바로** 열립니다(숨은 경로를 손으로 찾을 필요 없음).
3. `data.xml`을 고르면 **미리보기**(파일 내용 vs 현재 데이터)가 뜹니다. **교체**(파일 그대로) 또는 **병합**(현재+파일)을 고르면 서버에 올라갑니다.
4. 원본 파일은 지우지 않습니다. 안내는 `⋯ → XML 가져오기…`로도 언제든 다시 열 수 있습니다.

> 신규 사용자에게 빈 화면은 정상입니다 — 옮길 것이 없으면 그냥 쓰면 됩니다.

### 내보내기(반출·개인 백업)

- `⋯ → XML 내보내기` — 지금 데이터를 `taskCalendar` XML v1 파일로 저장합니다(포맷은 [SPEC.md](SPEC.md) §3). 저장 형식이 아니라 **반출 형식**입니다.
- `⋯ → 과제 데이터 내보내기…` — 과제 하나를 골라 **캘린더 기록(md) + 내 커밋 patch**를 폴더로 뽑습니다(위젯 전용, LLM 입력·실적 정리용).

## 14. 문제 해결(FAQ)

- **캘린더가 비어 있어요 / 내 기록이 사라졌어요** → 두 가지 중 하나입니다. ① **아직 안 옮긴 경우** — 화면 위 안내에서 [13. 옛 버전 기록 옮기기](#옛-버전-기록-옮기기-한-번만)를 진행하세요. ② **서버에 못 붙은 경우** — 「DB에서 캘린더를 읽지 못했습니다」 안내가 함께 뜹니다. 아래 항목을 보세요.
- **「DB에서 캘린더를 읽지 못했습니다 · 위젯을 다시 시작하세요」** → 아침에 PC가 사내망보다 먼저 뜨면 날 수 있습니다. **위젯을 껐다 켜면** 다시 조회합니다. (앱 안 [다시 시도] 버튼과 자동 재시도는 아직 없습니다.) 계속 실패하면 사내망 연결과 서버 상태를 확인하세요.
- **오늘 알림이 안 울렸어요** → 알림은 **서버에서 읽어 온 일정**을 대상으로만 돕니다. 그날 서버에 못 붙었다면 알림도 울리지 않습니다(의도된 동작 — 로컬에 알림표를 따로 두면 다른 PC에서 지운 일정이 여기서 울리게 됩니다).
- **로그인이 안 돼요** → **주간보고(netcus)와 같은 계정**입니다. 회사 **이메일 주소가 아닙니다** — 이메일을 넣으면 앱이 쓸 ID를 알려 줍니다. 그래도 안 되면 그 계정이 과제 DB(`app_user`)에 등록돼 있는지 관리자에게 확인하세요.
- **저장이 안 되고 "권한이 없습니다"가 떠요** → 공식 과제 편집 권한은 **로그인 계정**(`app_user.edit_role`)으로 정해집니다. 공용 관리자 비밀번호는 v0.17.0에서 폐지됐습니다. 👤 사용자 정보에서 지금 내 권한을 확인할 수 있습니다.
- **위젯이 안 떠요 / WebView2 오류** → Microsoft Edge WebView2 런타임이 필요합니다(Windows 11 기본 내장). 없으면 설치 후 재실행.
- **Git 커밋 불러오기 버튼이 없어요** → 위젯 모드 + 과제에 Git 저장소 경로 연결 + PC에 git 설치가 필요합니다(브라우저 체험판에는 없음). **자리를 옮겼다면 경로를 다시 연결**하세요(경로는 PC마다 따로 보관합니다).
- **새 버전을 배포했는데 안 바뀌어요** → 로그인 자동시작이 옛 빌드 경로를 가리킬 수 있습니다([ISSUES.md](ISSUES.md) #1). 자동시작 경로를 새 exe로 갱신하세요.
- **글자가 작아요/좁아요** → 가장자리 드래그로 창을 키우거나 ⚙ 크기 프리셋을 쓰세요. 좁은 폭에선 우측 패널이 하단 시트로 바뀝니다.
